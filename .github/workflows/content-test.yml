name: Content tests

on:
  push:
    branches: ["feat/android-knowledge-v1"]
  pull_request:

jobs:
  content-test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: "22"
      - run: npm test
